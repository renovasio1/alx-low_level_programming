#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: int will be checked
 *
 * Return: (0) Sucess
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
	return (0);
}
